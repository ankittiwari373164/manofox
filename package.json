{
  "name": "manofox",
  "version": "1.0.0",
  "private": true,
  "description": "Manofox website — single Node.js app (Express API + React build) for Hostinger shared hosting.",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "build": "cd frontend && npm install && npm run build",
    "install:frontend": "cd frontend && npm install"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "mysql2": "^3.11.0"
  },
  "engines": {
    "node": ">=18"
  }
}
